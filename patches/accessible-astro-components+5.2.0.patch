diff --git a/node_modules/accessible-astro-components/src/components/accordion/AccordionItem.astro b/node_modules/accessible-astro-components/src/components/accordion/AccordionItem.astro
index c1f389f..c6d0cdd 100644
--- a/node_modules/accessible-astro-components/src/components/accordion/AccordionItem.astro
+++ b/node_modules/accessible-astro-components/src/components/accordion/AccordionItem.astro
@@ -17,7 +17,7 @@ interface Props {
    */
   title: string
   /**
-   * Optional name attribute for the details element
+   * Optional name attribute for grouped accordion behavior
    */
   name?: string
   /**
@@ -56,12 +56,30 @@ const {
   open = false,
   ...rest
 } = Astro.props
+
+const slug = title
+  .toLowerCase()
+  .replace(/[^a-z0-9]+/g, '-')
+  .replace(/^-|-$/g, '')
+const uid = `${slug || 'accordion-item'}-${Math.random().toString(36).slice(2, 8)}`
+const triggerId = `${uid}-trigger`
+const panelId = `${uid}-panel`
 ---
 
 <li class:list={['item', className]} {...rest}>
-  <details class:list={['wrapper', `variant-${variant}`]} name={name} {...open ? { open } : {}}>
-    <summary class="title">
-      <Heading level={headingLevel} size={headingSize}>
+  <div
+    class:list={['wrapper', `variant-${variant}`]}
+    data-accordion-name={name}
+    data-open={open ? 'true' : 'false'}
+  >
+    <Heading level={headingLevel} size={headingSize} class="title-heading">
+      <button
+        id={triggerId}
+        class="title"
+        type="button"
+        aria-expanded={open ? 'true' : 'false'}
+        aria-controls={panelId}
+      >
         {
           variant === 'chevron' ? (
             <>
@@ -104,77 +122,181 @@ const {
             </>
           )
         }
-      </Heading>
-    </summary>
-    <div class="content">
-      <slot />
+      </button>
+    </Heading>
+    <div
+      id={panelId}
+      class="content"
+      role="region"
+      aria-labelledby={triggerId}
+      hidden={!open}
+      style={open ? undefined : 'max-height: 0; opacity: 0;'}
+    >
+      <div class="content-inner">
+        <slot />
+      </div>
     </div>
-  </details>
+  </div>
 </li>
 
+<script>
+  type AccordionWrapper = HTMLDivElement & { dataset: DOMStringMap }
+
+  const prefersReducedMotion = () => window.matchMedia('(prefers-reduced-motion: reduce)').matches
+
+  const getContentElements = (wrapper: AccordionWrapper) => {
+    const trigger = wrapper.querySelector<HTMLButtonElement>('.title')
+    const content = wrapper.querySelector<HTMLElement>('.content')
+    const contentInner = wrapper.querySelector<HTMLElement>('.content-inner')
+    return { trigger, content, contentInner }
+  }
+
+  const syncOpenState = (wrapper: AccordionWrapper, isOpen: boolean) => {
+    const { trigger, content, contentInner } = getContentElements(wrapper)
+    if (!trigger || !content || !contentInner) return
+
+    wrapper.dataset.open = isOpen ? 'true' : 'false'
+    trigger.setAttribute('aria-expanded', isOpen ? 'true' : 'false')
+
+    if (prefersReducedMotion()) {
+      content.hidden = !isOpen
+      content.style.maxHeight = isOpen ? 'none' : '0px'
+      content.style.opacity = isOpen ? '1' : '0'
+      return
+    }
+
+    if (isOpen) {
+      content.hidden = false
+      content.style.maxHeight = '0px'
+      content.style.opacity = '0'
+
+      requestAnimationFrame(() => {
+        content.style.maxHeight = `${contentInner.scrollHeight}px`
+        content.style.opacity = '1'
+      })
+
+      const onOpenEnd = (event: TransitionEvent) => {
+        if (event.propertyName !== 'max-height') return
+        content.style.maxHeight = 'none'
+        content.removeEventListener('transitionend', onOpenEnd)
+      }
+
+      content.addEventListener('transitionend', onOpenEnd)
+      return
+    }
+
+    const currentHeight = contentInner.scrollHeight
+    content.hidden = false
+    content.style.maxHeight = `${currentHeight}px`
+    content.style.opacity = '1'
+
+    requestAnimationFrame(() => {
+      content.style.maxHeight = '0px'
+      content.style.opacity = '0'
+    })
+
+    const onCloseEnd = (event: TransitionEvent) => {
+      if (event.propertyName !== 'max-height') return
+      content.hidden = true
+      content.removeEventListener('transitionend', onCloseEnd)
+    }
+
+    content.addEventListener('transitionend', onCloseEnd)
+  }
+
+  const initializeAccordionItems = () => {
+    document.querySelectorAll<AccordionWrapper>('.accordion .wrapper').forEach((wrapper) => {
+      if (wrapper.dataset.accordionBound === 'true') return
+
+      const { trigger } = getContentElements(wrapper)
+      if (!trigger) return
+
+      trigger.addEventListener('click', () => {
+        const isOpen = wrapper.dataset.open === 'true'
+        const groupName = wrapper.dataset.accordionName
+
+        if (groupName && !isOpen) {
+          document.querySelectorAll<AccordionWrapper>(`.accordion .wrapper[data-accordion-name="${groupName}"]`).forEach((item) => {
+            if (item !== wrapper && item.dataset.open === 'true') {
+              syncOpenState(item, false)
+            }
+          })
+        }
+
+        syncOpenState(wrapper, !isOpen)
+      })
+
+      const isInitiallyOpen = wrapper.dataset.open === 'true'
+      syncOpenState(wrapper, isInitiallyOpen)
+      wrapper.dataset.accordionBound = 'true'
+    })
+  }
+
+  initializeAccordionItems()
+
+  document.addEventListener('astro:after-swap', () => {
+    initializeAccordionItems()
+  })
+</script>
+
 <style>
   :where(.wrapper) {
     --transition-duration: 0.3s;
     --transition-easing: cubic-bezier(0.165, 0.84, 0.44, 1);
 
-    border-block-start: 2px solid light-dark(hsl(0 0% 13%), hsl(0 0% 87%));
+    border-block-start: var(--border-width-accordion, 2px) solid light-dark(hsl(0 0% 13%), hsl(0 0% 87%));
     padding-block: 0;
   }
 
-  .title :global(.heading) {
-    display: flex;
-    align-items: center;
-    gap: 1rem;
-    min-inline-size: 100%;
+  .title-heading {
+    margin: 0;
+  }
+
+  .title-heading :global(.heading) {
     font-weight: normal;
     font-size: 1.25rem;
   }
 
-  @media (prefers-reduced-motion: no-preference) {
-    :where(.wrapper) {
-      interpolate-size: allow-keywords;
-    }
+  :where(.title) {
+    display: flex;
+    align-items: center;
+    gap: 1rem;
+    cursor: pointer;
+    border: none;
+    background: transparent;
+    padding-block: 0.75rem;
+    padding-inline: 0;
+    min-inline-size: 100%;
+    color: inherit;
+    text-align: start;
+    font: inherit;
   }
 
-  details::details-content {
+  :where(.content) {
+    overflow: hidden;
     opacity: 0;
-    transition: content-visibility var(--transition-duration) allow-discrete,
-      opacity var(--transition-duration) var(--transition-easing),
-      block-size var(--transition-duration) var(--transition-easing);
-    block-size: 0;
-    overflow-y: clip;
   }
 
-  details[open]::details-content {
-    opacity: 1;
-    block-size: auto;
+  @media (prefers-reduced-motion: no-preference) {
+    :where(.content) {
+      transition:
+        max-height var(--transition-duration) var(--transition-easing),
+        opacity var(--transition-duration) var(--transition-easing);
+    }
   }
 
-  .wrapper.variant-default .content {
+  .wrapper.variant-default .content-inner {
     padding: 0 0 1.5rem 2.2rem;
   }
 
-  .wrapper.variant-chevron .title :global(.heading) {
+  .wrapper.variant-chevron .title {
     justify-content: space-between;
   }
 
-  .wrapper.variant-chevron .content {
+  .wrapper.variant-chevron .content-inner {
     padding-block-end: 2.2rem;
   }
 
-  :where(.title) {
-    display: flex;
-    cursor: pointer;
-    padding-block: 0.75rem;
-    list-style: none;
-  }
-
-  :where(.title)::marker,
-  :where(.title)::-webkit-details-marker {
-    display: none;
-    content: '';
-  }
-
   :where(.title:hover),
   :where(.title:focus-visible) {
     color: light-dark(hsl(215 25% 27%), hsl(215 25% 89%));
@@ -229,12 +351,12 @@ const {
     margin-block-start: 0.75rem;
   }
 
-  :where(.wrapper[open] .icon-plus svg:last-of-type) {
+  :where(.wrapper[data-open='true'] .icon-plus svg:last-of-type) {
     transform: rotate(90deg);
     inset-block-start: 0;
   }
 
-  :where(.wrapper[open] .icon-chevron svg) {
+  :where(.wrapper[data-open='true'] .icon-chevron svg) {
     transform: scaleY(-1);
   }
 </style>
diff --git a/node_modules/accessible-astro-components/src/components/avatar/Avatar.astro b/node_modules/accessible-astro-components/src/components/avatar/Avatar.astro
index 75c1527..7bf1700 100644
--- a/node_modules/accessible-astro-components/src/components/avatar/Avatar.astro
+++ b/node_modules/accessible-astro-components/src/components/avatar/Avatar.astro
@@ -201,7 +201,7 @@ const sizeValue = getSizeValue(size)
   /* Default type */
   :where(.initials-container),
   :where(.icon-container) {
-    border: 2px solid;
+    border: var(--border-width-avatar, 2px) solid;
     border-color: var(--color-default-border);
     background-color: var(--color-default-bg);
     color: var(--color-default-text);
diff --git a/node_modules/accessible-astro-components/src/components/card/Card.astro b/node_modules/accessible-astro-components/src/components/card/Card.astro
index 54bf207..79ec263 100644
--- a/node_modules/accessible-astro-components/src/components/card/Card.astro
+++ b/node_modules/accessible-astro-components/src/components/card/Card.astro
@@ -126,7 +126,7 @@ const {
     display: flex;
     position: relative;
     flex-direction: column;
-    border: 2px solid light-dark(hsl(0 0% 10%), hsl(0 0% 90%));
+    border: var(--border-width-card, 2px) solid light-dark(hsl(0 0% 10%), hsl(0 0% 90%));
     border-radius: 0.5rem;
     max-inline-size: 60ch;
     max-block-size: 100%;
diff --git a/node_modules/accessible-astro-components/src/components/modal/Modal.astro b/node_modules/accessible-astro-components/src/components/modal/Modal.astro
index a0fdf9d..4ea9c06 100644
--- a/node_modules/accessible-astro-components/src/components/modal/Modal.astro
+++ b/node_modules/accessible-astro-components/src/components/modal/Modal.astro
@@ -225,30 +225,23 @@ const titleId = `${triggerId}-title`
   }
 
   @media (prefers-reduced-motion: no-preference) {
-    :where(.modal) {
-      translate: 0 var(--translate-offset-y);
-      transition-behavior: allow-discrete;
-      transition-duration: var(--transition-duration);
-      transition-property: display, opacity, translate;
-      transition-timing-function: var(--transition-easing);
+    :where(.modal-inner) {
+      transform: translateY(var(--translate-offset-y));
+      opacity: 0;
+      transition:
+        transform var(--transition-duration) var(--transition-easing),
+        opacity var(--transition-duration) var(--transition-easing);
     }
   }
 
   .modal[open] {
     display: block;
-    translate: 0 0;
     opacity: 1;
   }
 
-  @starting-style {
-    .modal[open] {
-      translate: 0 calc(var(--translate-offset-y) * -1);
-      opacity: 0;
-    }
-
-    .modal[open]::backdrop {
-      opacity: 0;
-    }
+  .modal[open] .modal-inner {
+    transform: translateY(0);
+    opacity: 1;
   }
 
   .modal[open]::backdrop {
@@ -263,10 +256,7 @@ const titleId = `${triggerId}-title`
 
   @media (prefers-reduced-motion: no-preference) {
     :where(.modal)::backdrop {
-      transition-behavior: allow-discrete;
-      transition-duration: 0.3s;
-      transition-property: display, opacity, overlay;
-      transition-timing-function: cubic-bezier(0.165, 0.84, 0.44, 1);
+      transition: opacity 0.3s cubic-bezier(0.165, 0.84, 0.44, 1);
     }
   }
 
diff --git a/node_modules/accessible-astro-components/src/components/tooltip/Tooltip.astro b/node_modules/accessible-astro-components/src/components/tooltip/Tooltip.astro
index 9ac20de..994562a 100644
--- a/node_modules/accessible-astro-components/src/components/tooltip/Tooltip.astro
+++ b/node_modules/accessible-astro-components/src/components/tooltip/Tooltip.astro
@@ -182,13 +182,12 @@ const inlineStyle = `--tooltip-anchor: ${anchorName}; --tooltip-offset: ${offset
     position-anchor: var(--tooltip-anchor);
     position-area: top;
     position-try-fallbacks: flip-block, flip-inline;
-    translate: 0 var(--translate-offset-y);
+    transform: translateY(var(--translate-offset-y));
     opacity: 0;
     z-index: var(--z-index-6);
-    transition-behavior: allow-discrete;
-    transition-duration: var(--transition-duration);
-    transition-property: opacity, translate, overlay, display;
-    transition-timing-function: var(--transition-easing);
+    transition:
+      opacity var(--transition-duration) var(--transition-easing),
+      transform var(--transition-duration) var(--transition-easing);
     box-shadow: var(--elevation-1);
     border: 1px solid var(--color-default-border);
     border-radius: var(--radius-s);
@@ -198,58 +197,26 @@ const inlineStyle = `--tooltip-anchor: ${anchorName}; --tooltip-offset: ${offset
   }
 
   :where(.tooltip.position-top) {
-    translate: 0 calc(var(--translate-offset-y) * -1);
+    transform: translateY(calc(var(--translate-offset-y) * -1));
   }
 
   :where(.tooltip.position-bottom) {
-    translate: 0 var(--translate-offset-y);
+    transform: translateY(var(--translate-offset-y));
   }
 
   :where(.tooltip.position-left) {
-    translate: var(--translate-offset-x) 0;
+    transform: translateX(var(--translate-offset-x));
   }
 
   :where(.tooltip.position-right) {
-    translate: calc(var(--translate-offset-x) * -1) 0;
+    transform: translateX(calc(var(--translate-offset-x) * -1));
   }
 
   :where(.tooltip:popover-open) {
-    translate: 0 0;
+    transform: translate(0, 0);
     opacity: 1;
   }
 
-  /* Default position (top) */
-  @starting-style {
-    :where(.tooltip.position-top:popover-open) {
-      translate: 0 var(--translate-offset-y);
-      opacity: 0;
-    }
-  }
-
-  /* Right position */
-  @starting-style {
-    :where(.tooltip.position-right:popover-open) {
-      translate: calc(var(--translate-offset-x) * -1) 0;
-      opacity: 0;
-    }
-  }
-
-  /* Bottom position */
-  @starting-style {
-    :where(.tooltip.position-bottom:popover-open) {
-      translate: 0 calc(var(--translate-offset-y) * -1);
-      opacity: 0;
-    }
-  }
-
-  /* Left position */
-  @starting-style {
-    :where(.tooltip.position-left:popover-open) {
-      translate: var(--translate-offset-x) 0;
-      opacity: 0;
-    }
-  }
-
   @media (prefers-reduced-motion: reduce) {
     :where(.tooltip) {
       transition: none;
